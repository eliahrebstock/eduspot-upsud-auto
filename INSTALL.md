This script is run by Python 3 and use _requests_ and _BeautifulSoup_ librairies.

First install [pipenv](https://docs.pipenv.org/).

Then use :

```
$ pipenv --three
```

```
$ pipenv install
```

```
$ pipenv run upsud.py ...
```